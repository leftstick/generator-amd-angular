/**
 *  Entrance of services
 *
 *  @author  <%= answers.username %>
 *  @date    <%= answers.date %>
 *
 */
'use strict';

define(['./Events', './Utils'], function() {
    return [].slice.call(arguments);
});
