/**
 *  Entrance of services
 *
 *  @author  Hao.Zuo
 *  @date    Apr 28th, 2015
 *
 */
(function(define) {
    'use strict';

    define([
        './Events',
        './Utils'
    ], function() {
        return [].slice.call(arguments, 0);
    });

}(define));
